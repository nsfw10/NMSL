# These rules will change if you change your site’s custom domains or HTTPS settings

# Redirect default Netlify subdomain to primary domain
https://vigorous-lovelace-564ced.netlify.com/* https://cxh.eroero.me/:splat 301!

# These extra rules are required because HTTPS is not forced
http://vigorous-lovelace-564ced.netlify.com/* https://cxh.eroero.me/:splat 301!